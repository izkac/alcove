# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist.locked
dist-ssr
*.local
/target/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Updater signing. The private key lives in ~/.tauri and must never land here;
# these patterns are a backstop for a stray copy or an env file holding it.
*.key
*.key.pub
.env
.env.*

# marketing listener - local state, not code
scripts/leads.md
scripts/listen.seen.json
scripts/listen.feeds.txt

# Out-of-tree release build output
/src-tauri/target-rel/

# Local agent tooling, not project code
.agents/
.claude/
.codex/
.cursor/
.forge/
.impeccable/
.opencode/
